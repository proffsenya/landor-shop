# Docker Compose для фронтенда
# Запускать из корня проекта: docker-compose up -d --build
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: landor-frontend
    ports:
      - "3000:80"
    restart: unless-stopped


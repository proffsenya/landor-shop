/**
 * Простой локальный поиск по каталогу, избранному, корзине и т.д.
 * Ищет подстроку без учёта регистра, без чувствительности к знакам препинания
 * и с поддержкой перестановки слов.
 *
 * @param {string} query - строка поиска
 * @param {Array} dataset - массив объектов (например, карточек товаров)
 * @param {number} [limit=15] - максимум результатов
 * @returns {Array<{id, title, subtitle, url, image, type}>}
 */
// Функция нормализации е/ё и других символов для поиска
const normalizeE = (str) => {
  if (!str || typeof str !== 'string') return '';
  return str
    .replace(/ё/g, 'е')
    .replace(/Ё/g, 'Е')
    .replace(/\u200B/g, '') // Убираем невидимые пробелы
    .replace(/\u00A0/g, ' ') // Заменяем неразрывные пробелы на обычные
    .trim();
};

// Функция для расширения запроса синонимами и вариантами
const expandQuery = (query) => {
  const normalized = normalizeE(query.toLowerCase().trim());
  
  // Словарь синонимов и вариантов
  const synonyms = {
    // Общие слова
    'все': ['всех', 'всем', 'всеми', 'всего', 'для всех'],
    'для всех': ['все', 'всех', 'всем', 'всеми', 'всего'],
    'породы': ['пород', 'породам', 'породами', 'породе'],
    'все породы': ['для всех', 'для всех пород', 'всех пород', 'всем породам', 'все пород', 'все породы'],
    
    // Кошки
    'кошка': ['кошки', 'кошек', 'кошкам', 'кошками', 'кошке', 'для кошек', 'для кошек'],
    'кошки': ['кошка', 'кошек', 'кошкам', 'кошками', 'кошке', 'для кошек'],
    'кошек': ['кошка', 'кошки', 'кошкам', 'кошками', 'кошке', 'для кошек'],
    'для кошек': ['кошка', 'кошки', 'кошек', 'кошкам', 'кошками'],
    
    // Котята
    'котенок': ['котят', 'котенка', 'котенку', 'котенком', 'котятам', 'для котят', 'котенка', 'котенку'],
    'котят': ['котенок', 'котенка', 'котенку', 'котенком', 'котятам', 'для котят'],
    'для котят': ['котенок', 'котенка', 'котенку', 'котенком', 'котятам', 'котят'],
    
    // Собаки
    'собака': ['собаки', 'собак', 'собакам', 'собаками', 'собаке', 'для собак', 'для собак'],
    'собаки': ['собака', 'собак', 'собакам', 'собаками', 'собаке', 'для собак'],
    'собак': ['собака', 'собаки', 'собакам', 'собаками', 'собаке', 'для собак'],
    'для собак': ['собака', 'собаки', 'собак', 'собакам', 'собаками'],
    
    // Щенки
    'щенок': ['щенков', 'щенка', 'щенку', 'щенком', 'щенкам', 'для щенков', 'щенка', 'щенку', "щенки"],
    'щенков': ['щенок', 'щенка', 'щенку', 'щенком', 'щенкам', 'для щенков', "щенки"],
    'для щенков': ['щенок', 'щенка', 'щенку', 'щенком', 'щенкам', 'щенков', "щенки"],
    
    // Породы собак - мелкие
    'мелкие': ['мелких', 'мелким', 'мелкие породы', 'для мелких пород', 'мелких пород'],
    'мелких': ['мелкие', 'мелким', 'мелкие породы', 'для мелких пород'],
    'мелкие породы': ['мелких пород', 'для мелких пород', 'мелким породам', 'мелкие пород', 'мелкие', 'мелких'],
    'мелких пород': ['мелкие породы', 'для мелких пород', 'мелким породам', 'мелкие', 'мелких'],
    'для мелких пород': ['мелкие породы', 'мелких пород', 'мелким породам', 'мелкие', 'мелких'],
    'мелким породам': ['мелкие породы', 'мелких пород', 'для мелких пород', 'мелкие'],
    
    // Породы собак - средние
    'средние': ['средних', 'средним', 'средние породы', 'для средних пород', 'средних пород'],
    'средних': ['средние', 'средним', 'средние породы', 'для средних пород'],
    'средние породы': ['средних пород', 'для средних пород', 'средним породам', 'средние пород', 'средние', 'средних'],
    'средних пород': ['средние породы', 'для средних пород', 'средним породам', 'средние', 'средних'],
    'для средних пород': ['средние породы', 'средних пород', 'средним породам', 'средние', 'средних'],
    'средним породам': ['средние породы', 'средних пород', 'для средних пород', 'средние'],
    
    // Породы собак - крупные
    'крупные': ['крупных', 'крупным', 'крупные породы', 'для крупных пород', 'крупных пород'],
    'крупных': ['крупные', 'крупным', 'крупные породы', 'для крупных пород'],
    'крупные породы': ['крупных пород', 'для крупных пород', 'крупным породам', 'крупные пород', 'крупные', 'крупных'],
    'крупных пород': ['крупные породы', 'для крупных пород', 'крупным породам', 'крупные', 'крупных'],
    'для крупных пород': ['крупные породы', 'крупных пород', 'крупным породам', 'крупные', 'крупных'],
    'крупным породам': ['крупные породы', 'крупных пород', 'для крупных пород', 'крупные'],
    
    // Стерилизованные
    'стерилизованные': ['стерилизованным', 'стерилизованным кошкам', 'для стерилизованных', 'для стерилизованных кошек', 'стерилизованным котам'],
    'стерилизованных': ['стерилизованным', 'стерилизованным кошкам', 'для стерилизованных', 'стерилизованные', 'стерилизованным котам'],
    'для стерилизованных': ['стерилизованным', 'стерилизованным кошкам', 'для стерилизованных кошек', 'стерилизованные', 'стерилизованным котам'],
    'стерилизованным': ['стерилизованные', 'стерилизованных', 'для стерилизованных'],
    
    // Здоровье кожи и шерсти
    'для здоровья кожи и шерсти': ['здоровье кожи', 'кожа и шерсть', 'для кожи', 'для шерсти', 'здоровье шерсти'],
    'здоровье кожи': ['для здоровья кожи и шерсти', 'кожа и шерсть', 'для кожи'],
    'кожа и шерсть': ['для здоровья кожи и шерсти', 'здоровье кожи', 'для шерсти'],
    'для кожи': ['для здоровья кожи и шерсти', 'здоровье кожи', 'кожа и шерсть'],
    'для шерсти': ['для здоровья кожи и шерсти', 'кожа и шерсть', 'здоровье шерсти'],
    
    // Чувствительный
    'чувствительный': ['чувствительного', 'чувствительному', 'чувствительным', 'чувствительная', 'чувствительной', 'для чувствительного', 'с чувствительным'],
    'чувствительного': ['чувствительный', 'чувствительному', 'чувствительным', 'для чувствительного', 'с чувствительным'],
    'для чувствительного': ['чувствительный', 'чувствительного', 'чувствительному', 'чувствительным', 'с чувствительным'],
    'для чувствительного пищеварения': ['чувствительный', 'чувствительного', 'чувствительное пищеварение', 'для чувствительного', 'чувствительным'],
    'чувствительное пищеварение': ['для чувствительного пищеварения', 'чувствительный', 'чувствительного'],
    'с чувствительным': ['чувствительный', 'чувствительного', 'чувствительному', 'чувствительным', 'для чувствительного'],
    'чувствительным': ['чувствительный', 'чувствительного', 'чувствительному', 'для чувствительного'],
    
    // Привередливые
    'для привередливых': ['привередливые', 'привередливым', 'привередливых'],
    'привередливые': ['для привередливых', 'привередливым', 'привередливых'],
    'привередливым': ['для привередливых', 'привередливые', 'привередливых'],
    'привередливых': ['для привередливых', 'привередливые', 'привередливым'],
    
    // Домашние (indoor)
    'для домашних': ['домашние', 'домашним', 'индор', 'для домашних кошек', 'индор кошки'],
    'домашние': ['для домашних', 'домашним', 'индор'],
    'домашним': ['для домашних', 'домашние'],
    'индор': ['для домашних', 'домашние', 'индор кошки'],
    'indoor': ['для домашних', 'домашние', 'индор'],
    
    // Типы корма
    'сухой': ['сухого', 'сухому', 'сухим', 'сухого корма', 'сухой корм'],
    'сухого': ['сухой', 'сухому', 'сухим', 'сухого корма'],
    'сухой корм': ['сухой', 'сухого', 'сухого корма'],
    'сухого корма': ['сухой', 'сухого', 'сухой корм'],
    
    'влажный': ['влажного', 'влажному', 'влажным', 'влажного корма', 'влажный корм'],
    'влажного': ['влажный', 'влажному', 'влажным', 'влажного корма'],
    'влажный корм': ['влажный', 'влажного', 'влажного корма'],
    'влажного корма': ['влажный', 'влажного', 'влажный корм'],
    
    'консервы': ['консерв', 'консервам', 'консервами', 'консервов'],
    'консерв': ['консервы', 'консервам', 'консервов'],
    
    // Типы продуктов
    'корм': ['корма', 'корму', 'кормом', 'кормам', 'кормами'],
    'корма': ['корм', 'корму', 'кормом'],
    
    'наполнитель': ['наполнителя', 'наполнителю', 'наполнителем', 'наполнителям'],
    'наполнителя': ['наполнитель', 'наполнителю', 'наполнителем'],
    
    'аксессуары': ['аксессуар', 'аксессуаров', 'аксессуарам', 'аксессуарами'],
    'аксессуар': ['аксессуары', 'аксессуаров', 'аксессуарам'],
    
    // Вкусы - мясо
    'кролик': ['кролика', 'кролику', 'кроликом'],
    'кролика': ['кролик', 'кролику', 'кроликом'],
    
    'курица': ['курицы', 'курице', 'курицей', 'куриц', 'курицам', 'куриное', 'куриного'],
    'курицы': ['курица', 'курице', 'курицей'],
    'куриное': ['курица', 'курицы', 'куриного'],
    'куриного': ['курица', 'курицы', 'куриное'],
    
    'куропатка': ['куропатки', 'куропатке', 'куропаткой'],
    'куропатки': ['куропатка', 'куропатке', 'куропаткой'],
    
    'перепелка': ['перепелки', 'перепелке', 'перепелкой', 'перепелок'],
    'перепелки': ['перепелка', 'перепелке', 'перепелкой'],
    
    'телятина': ['телятины', 'телятине', 'телятиной'],
    'телятины': ['телятина', 'телятине', 'телятиной'],
    
    'ягненок': ['ягненка', 'ягненку', 'ягненком', 'ягнят', 'ягнята'],
    'ягненка': ['ягненок', 'ягненку', 'ягненком'],
    'ягнята': ['ягненок', 'ягненка', 'ягнят'],
    
    'гусь': ['гуся', 'гусю', 'гусем', 'гусей'],
    'гуся': ['гусь', 'гусю', 'гусем'],
    
    'утка': ['утки', 'утке', 'уткой', 'уток'],
    'утки': ['утка', 'утке', 'уткой'],
    
    'говядина': ['говядины', 'говядине', 'говядиной', 'говяжье', 'говяжьего'],
    'говядины': ['говядина', 'говядине', 'говядиной'],
    'говяжье': ['говядина', 'говядины', 'говяжьего'],
    'говяжьего': ['говядина', 'говядины', 'говяжье'],
    
    'индейка': ['индейки', 'индейке', 'индейкой', 'индеек', 'индюшатина', 'индюшатины'],
    'индейки': ['индейка', 'индейке', 'индейкой'],
    'индюшатина': ['индейка', 'индейки', 'индюшатины'],
    
    // Вкусы - рыба
    'лосось': ['лосося', 'лососю', 'лососем', 'лососей', 'лососевый', 'лососевого'],
    'лосося': ['лосось', 'лососю', 'лососем'],
    'лососевый': ['лосось', 'лосося', 'лососевого'],
    
    'рыба': ['рыбы', 'рыбе', 'рыбой', 'рыб', 'рыбам', 'рыбный', 'рыбного'],
    'рыбы': ['рыба', 'рыбе', 'рыбой'],
    'рыбный': ['рыба', 'рыбы', 'рыбного'],
    
    // Запахи
    'классический': ['классического', 'классическому', 'классическим', 'классическая', 'классической'],
    'классического': ['классический', 'классическому', 'классическим'],
    
    'ванильный': ['ванильного', 'ванильному', 'ванильным', 'ванильная', 'ванильной', 'ваниль'],
    'ванильного': ['ванильный', 'ванильному', 'ванильным'],
    'ваниль': ['ванильный', 'ванильного'],
    
    'банановый': ['бананового', 'банановому', 'банановым', 'банановая', 'банановой', 'банан'],
    'бананового': ['банановый', 'банановому', 'банановым'],
    'банан': ['банановый', 'бананового'],
    
    'кокосовый': ['кокосового', 'кокосовому', 'кокосовым', 'кокосовая', 'кокосовой', 'кокос'],
    'кокосового': ['кокосовый', 'кокосовому', 'кокосовым'],
    'кокос': ['кокосовый', 'кокосового'],
    
    'зеленый чай': ['зеленого чая', 'зеленому чаю', 'зеленым чаем'],
    'зеленого чая': ['зеленый чай', 'зеленому чаю', 'зеленым чаем'],
    
    'аромат розы': ['аромата розы', 'аромату розы', 'ароматом розы', 'роза', 'розовый', 'розового'],
    'аромата розы': ['аромат розы', 'аромату розы', 'ароматом розы'],
    'роза': ['аромат розы', 'аромата розы', 'розовый'],
    'розовый': ['аромат розы', 'аромата розы', 'роза'],
    
    'яблоко': ['яблока', 'яблоку', 'яблоком', 'яблок', 'яблочный', 'яблочного'],
    'яблока': ['яблоко', 'яблоку', 'яблоком'],
    'яблочный': ['яблоко', 'яблока', 'яблочного'],
    
    'лимон': ['лимона', 'лимону', 'лимоном', 'лимоны', 'лимонов', 'лимонный', 'лимонного'],
    'лимона': ['лимон', 'лимону', 'лимоном'],
    'лимонный': ['лимон', 'лимона', 'лимонного'],
    
    'без аромата': ['без запаха', 'нейтральный'],
    'без запаха': ['без аромата', 'нейтральный'],
    'нейтральный': ['без аромата', 'без запаха'],
    
    'молоко': ['молока', 'молоку', 'молоком', 'молочный', 'молочного'],
    'молока': ['молоко', 'молоку', 'молоком'],
    'молочный': ['молоко', 'молока', 'молочного'],
    
    'лаванда': ['лаванды', 'лаванде', 'лавандой', 'лавандовый', 'лавандового'],
    'лаванды': ['лаванда', 'лаванде', 'лавандой'],
    'лавандовый': ['лаванда', 'лаванды', 'лавандового'],
    
    // Страны
    'испания': ['испании', 'испанский', 'испанского'],
    'испании': ['испания', 'испанский'],
    'испанский': ['испания', 'испании'],
    
    'германия': ['германии', 'немецкий', 'немецкого'],
    'германии': ['германия', 'немецкий'],
    'немецкий': ['германия', 'германии'],
    
    'россия': ['россии', 'российский', 'российского'],
    'россии': ['россия', 'российский'],
    'российский': ['россия', 'россии'],
    
    'беларусь': ['беларуси', 'белорусский', 'белорусского'],
    'беларуси': ['беларусь', 'белорусский'],
    'белорусский': ['беларусь', 'беларуси'],
    
    'китай': ['китая', 'китаю', 'китаем', 'китайский', 'китайского'],
    'китая': ['китай', 'китайский'],
    'китайский': ['китай', 'китая'],
    
    // Бренды
    'landor': ['ландор'],
    'ландор': ['landor'],
    
    'landy': ['ленди'],
    'ленди': ['landy'],
    
    'fresh pet profbalance': ['freshpet', 'profbalance', 'фреш пет'],
    'freshpet': ['fresh pet profbalance', 'profbalance'],
    'profbalance': ['fresh pet profbalance', 'freshpet'],
    
    'чистые пушистые': ['чистые', 'пушистые'],
    'чистые': ['чистые пушистые'],
    'пушистые': ['чистые пушистые'],
  };
  
  // Расширяем запрос синонимами
  let expanded = [normalized];
  
  // Проверяем полные фразы (сначала длинные, потом короткие)
  const sortedKeys = Object.keys(synonyms).sort((a, b) => b.length - a.length);
  for (const key of sortedKeys) {
    if (normalized.includes(key)) {
      const variants = synonyms[key];
      expanded.push(...variants);
      // Также добавляем комбинации
      variants.forEach(variant => {
        expanded.push(normalized.replace(key, variant));
      });
    }
  }
  
  // Проверяем отдельные слова
  const words = normalized.split(/\s+/);
  words.forEach(word => {
    if (synonyms[word]) {
      expanded.push(...synonyms[word]);
    }
  });
  
  // Убираем дубликаты и возвращаем уникальные варианты
  return [...new Set(expanded)];
};

export function localSearch(query, dataset = [], limit = 15) {
  // Более надежная нормализация запроса
  if (!query || (typeof query !== 'string' && typeof query !== 'number')) return [];
  const q = String(query).trim().toLowerCase();
  if (!q) return [];

  // Нормализуем е/ё в запросе
  const normalizedQuery = normalizeE(q);

  // Очищаем запрос от знаков препинания и разбиваем на слова
  const cleanQuery = normalizedQuery
    .replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, ' ')
    .replace(/\s+/g, ' ')
    .trim();

  if (!cleanQuery) return [];

  // Расширяем запрос синонимами
  const expandedQueries = expandQuery(cleanQuery);
  
  // Нормализуем и очищаем расширенные запросы
  const normalizedExpandedQueries = expandedQueries.map(eq => {
    const normalized = normalizeE(eq);
    return normalized
      .replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, ' ')
      .replace(/\s+/g, ' ')
      .trim();
  }).filter(eq => eq.length > 0);
  
  // Разбиваем запрос на отдельные слова для каждого варианта
  const queryWordsVariants = normalizedExpandedQueries.map(expandedQuery => 
    expandedQuery.split(' ').filter(word => word.length > 0)
  );
  
  // Берем основной вариант для проверки
  const queryWords = cleanQuery.split(' ').filter(word => word.length > 0);

  const res = [];
  for (const item of dataset) {
    // Проверяем, есть ли variants (старая структура) или это уже плоская карточка
    const variants = Array.isArray(item?.variants) ? item.variants : [];
    
    if (variants.length > 0) {
      // Старая структура: продукт с вариантами
      variants.forEach((variant) => {
        // Нормализуем все поля перед использованием
        const title = normalizeE(
          variant?.displayName || 
          variant?.display_name || 
          item?.productName || 
          item?.name || 
          ""
        );
        const desc = normalizeE(
          variant?.description || 
          item?.brand || 
          ""
        );
        
        // Формируем строку веса для поиска
        let weightStr = "";
        if (variant?.weight) {
          if (typeof variant.weight === "number") {
            // Добавляем разные варианты написания веса: "1", "1 кг", "1.0", "1,0" и т.д.
            const weightNum = variant.weight;
            const weightInt = Math.floor(weightNum);
            const weightDecimal = weightNum % 1 === 0 ? "" : weightNum.toFixed(3);
            weightStr = `${weightNum} ${weightInt} ${weightDecimal} кг ${weightNum.toString().replace('.', ',')} ${weightNum.toString().replace(',', '.')}`;
          } else {
            weightStr = String(variant.weight);
          }
        }
        
        const combined = `${title} ${desc} ${weightStr}`.toLowerCase().trim();

        // Нормализуем е/ё в тексте для поиска
        const normalizedCombined = normalizeE(combined);

        // Очищаем комбинированную строку так же как запрос
        const cleanCombined = normalizedCombined
          .replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, ' ')
          .replace(/\s+/g, ' ')
          .trim();

        // Проверяем, содержатся ли все слова запроса в тексте (в любом порядке)
        // Также проверяем расширенные варианты запроса
        const checkMatch = (words) => {
          if (!words || words.length === 0) return false;
          // Проверяем, что все слова найдены (более гибкая проверка)
          return words.every(word => {
            if (word.length < 2) return true; // Игнорируем очень короткие слова
            return cleanCombined.includes(word);
          });
        };
        
        // Также проверяем частичное совпадение (хотя бы одно слово)
        const checkPartialMatch = (words) => {
          if (!words || words.length === 0) return false;
          return words.some(word => word.length >= 2 && cleanCombined.includes(word));
        };
        
        const allWordsFound = checkMatch(queryWords) || 
          queryWordsVariants.some(variantWords => checkMatch(variantWords)) ||
          normalizedExpandedQueries.some(expandedQuery => cleanCombined.includes(expandedQuery)) ||
          // Дополнительная проверка: если запрос короткий (1-2 слова), используем частичное совпадение
          (queryWords.length <= 2 && checkPartialMatch(queryWords));

        if (allWordsFound) {
          const variantId = variant?.id ?? variant?.variantId;
          const productId = item?.id ?? item?.productId ?? item?.parentId;
          
          res.push({
            id: String(variantId ?? Math.random()),
            title: title || "Товар",
            subtitle: typeof variant?.price === "number" ? `${variant.price} ₽` : desc,
            image: variant?.imageUrl || item?.imageUrl || "/korm1.svg",
            url: productId && variantId
              ? `/product/${encodeURIComponent(productId)}?variant=${encodeURIComponent(variantId)}`
              : variantId
              ? `/product/${encodeURIComponent(variantId)}`
              : "#",
            type: "product",
          });
        }
        if (res.length >= limit) return;
      });
    } else {
      // Новая структура: плоская карточка (из /api/products/cards/search-by-url)
      // Собираем все возможные поля названия для более надежного поиска
      // Нормализуем все поля перед использованием
      const title = normalizeE(
        item?.displayName || 
        item?.display_name || 
        item?.productName || 
        item?.name || 
        item?.title || 
        ""
      );
      const desc = normalizeE(
        item?.description || 
        item?.brand || 
        item?.subtitle || 
        ""
      );
      
      // Формируем строку веса для поиска
      let weightStr = "";
      if (item?.weight) {
        if (typeof item.weight === "number") {
          // Добавляем разные варианты написания веса: "1", "1 кг", "1.0", "1,0" и т.д.
          const weightNum = item.weight;
          const weightInt = Math.floor(weightNum);
          const weightDecimal = weightNum % 1 === 0 ? "" : weightNum.toFixed(3);
          weightStr = `${weightNum} ${weightInt} ${weightDecimal} кг ${weightNum.toString().replace('.', ',')} ${weightNum.toString().replace(',', '.')}`;
        } else {
          weightStr = String(item.weight);
        }
      }
      if (item?.weightLabel) {
        // Извлекаем число из weightLabel для поиска
        const labelNum = item.weightLabel.match(/[\d,\.]+/);
        if (labelNum) {
          weightStr += ` ${labelNum[0]} ${labelNum[0].replace(',', '.')} ${labelNum[0].replace('.', ',')}`;
        }
        weightStr += ` ${item.weightLabel} ${item.weightLabel.replace('кг', '').replace(/\s+/g, ' ').trim()}`;
      }
      
      const combined = `${title} ${desc} ${weightStr}`.toLowerCase().trim();

      // Нормализуем е/ё в тексте для поиска
      const normalizedCombined = normalizeE(combined);

      // Очищаем комбинированную строку так же как запрос
      const cleanCombined = normalizedCombined
        .replace(/[.,\/#!$%\^&\*;:{}=\-_`~()]/g, ' ')
        .replace(/\s+/g, ' ')
        .trim();

      // Проверяем, содержатся ли все слова запроса в тексте (в любом порядке)
      // Также проверяем расширенные варианты запроса
      const checkMatch = (words) => {
        if (!words || words.length === 0) return false;
        // Проверяем, что все слова найдены (более гибкая проверка)
        return words.every(word => {
          if (word.length < 2) return true; // Игнорируем очень короткие слова
          return cleanCombined.includes(word);
        });
      };
      
      // Также проверяем частичное совпадение (хотя бы одно слово)
      const checkPartialMatch = (words) => {
        if (!words || words.length === 0) return false;
        return words.some(word => word.length >= 2 && cleanCombined.includes(word));
      };
      
      const allWordsFound = checkMatch(queryWords) || 
        queryWordsVariants.some(variantWords => checkMatch(variantWords)) ||
        normalizedExpandedQueries.some(expandedQuery => cleanCombined.includes(expandedQuery)) ||
        // Дополнительная проверка: если запрос короткий (1-2 слова), используем частичное совпадение
        (queryWords.length <= 2 && checkPartialMatch(queryWords));

      if (allWordsFound) {
        const variantId = item?.id ?? item?.variantId;
        const productId = item?.parentId ?? item?.productId ?? item?.productId;
        
        // Сохраняем все поля из исходных данных и дополняем их
        res.push({
          ...item, // Сохраняем все исходные поля
          id: String(variantId ?? item?.id ?? Math.random()),
          title: title || item?.title || "Товар",
          displayName: title || item?.displayName || "Товар",
          subtitle: typeof item?.price === "number" ? `${item.price} ₽` : (item?.subtitle || desc),
          image: item?.imageUrl || item?.image || "/korm1.svg",
          imageUrl: item?.imageUrl || item?.image || "/korm1.svg",
          price: item?.price ?? null,
          weight: item?.weight ?? null,
          weightLabel: item?.weightLabel || (item?.weight ? (typeof item.weight === "number" ? `${item.weight} кг` : item.weight) : null),
          productId: productId,
          variantId: variantId,
          url: item?.url || (productId && variantId
            ? `/product/${encodeURIComponent(productId)}?variant=${encodeURIComponent(variantId)}`
            : variantId
            ? `/product/${encodeURIComponent(variantId)}`
            : "#"),
          type: "product",
        });
      }
    }
    
    if (res.length >= limit) break;
  }

  return res;
}